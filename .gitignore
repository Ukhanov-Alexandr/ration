# Резервные копии данных пользователя — артефакт кнопки экспорта в приложении.
# Это личные данные, в публичный репозиторий они не попадают.
*.json
!manifest.json

# Локальные настройки редактора и агентов.
.claude/
